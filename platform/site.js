(()=>{const root=document.documentElement;const themeButton=document.getElementById('themeToggle');const menuButton=document.getElementById('menuToggle');const menu=document.getElementById('mobileMenu');const stored=localStorage.getItem('korual-theme');if(stored==='dark')root.dataset.theme='dark';const setTheme=()=>{const dark=root.dataset.theme==='dark';localStorage.setItem('korual-theme',dark?'dark':'light');const meta=document.querySelector('meta[name="theme-color"]');if(meta)meta.setAttribute('content',dark?'#090e17':'#f7f9fc')};setTheme();themeButton?.addEventListener('click',()=>{root.dataset.theme=root.dataset.theme==='dark'?'light':'dark';setTheme()});menuButton?.addEventListener('click',()=>{menu.hidden=!menu.hidden;menuButton.textContent=menu.hidden?'☰':'×'});menu?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{menu.hidden=true;menuButton.textContent='☰'}));const links=[...document.querySelectorAll('[data-nav]')];const sections=links.map(a=>document.getElementById(a.dataset.nav)).filter(Boolean);if('IntersectionObserver'in window){const io=new IntersectionObserver(entries=>{const hit=entries.filter(e=>e.isIntersecting).sort((a,b)=>b.intersectionRatio-a.intersectionRatio)[0];if(!hit)return;links.forEach(a=>a.classList.toggle('active',a.dataset.nav===hit.target.id))},{rootMargin:'-20% 0px -62% 0px',threshold:[0,.2,.5]});sections.forEach(s=>io.observe(s))}})();